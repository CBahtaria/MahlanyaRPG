# MahlanyaRPG Android Keystore Properties Template
# Copy to keystore.properties (git-ignored) and fill in real values.
# NEVER commit keystore.properties or the .jks file to version control.

storeFile=mahlanya_release.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=mahlanya_release_key
keyPassword=REPLACE_WITH_KEY_PASSWORD

# Keystore generation command:
# keytool -genkey -v -keystore mahlanya_release.jks \
#   -alias mahlanya_release_key -keyalg RSA -keysize 4096 \
#   -validity 10000 -storetype JKS
