# Mahlanya RPG — Offline Science Pipeline Dependencies
# Python >= 3.11 required
# Install: pip install -r requirements.txt
# GPU acceleration (optional): pip install cupy-cuda12x

# Geospatial
gdal>=3.8.0
rasterio>=1.3.0
shapely>=2.0.0
geopandas>=0.14.0
pyproj>=3.6.0

# Numerical / Science
numpy>=1.26.0
scipy>=1.12.0
numba>=0.59.0

# 3D mesh (acoustic IR geometry)
trimesh>=4.0.0

# Knowledge graph I/O
neo4j>=5.0.0

# CLI and serialisation
click>=8.1.0
pyyaml>=6.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional GPU acceleration (install separately with CUDA 12.x):
# cupy-cuda12x>=13.0.0
